spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      routes:
        - id: account-service
          uri: http://localhost:9000/
          predicates:
            - Path=/accounts/**
          filters:
            - RewritePath=/accounts/?(?<segment>.*), /$\{segment}
        - id: money-service
          uri: http://localhost:10000/
          predicates:
            - Path=/money/health/ping
            - Method=GET
          filters:
            - RewritePath=/money/?(?<segment>.*), /$\{segment}
        - id: money-service
          uri: http://localhost:10000/
          predicates:
            - Path=/money/**
          filters:
            - RewritePath=/money/?(?<segment>.*), /$\{segment}
            - PreAuthGatewayFilter
            - PostAuthGatewayFilter